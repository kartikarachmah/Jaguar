<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.albasyir.jundana.familytracer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="jp.tubes.pbp.jaguar.OrangTua.App.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="jp.tubes.pbp.jaguar.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key_map" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name="jp.tubes.pbp.jaguar.Anak.Login_Anak" />
        <activity android:name="jp.tubes.pbp.jaguar.Anak.Profile_Anak" />
        <activity
            android:name="jp.tubes.pbp.jaguar.OrangTua.Activity.MainOrangtua"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="jp.tubes.pbp.jaguar.OrangTua.Activity.Detail_Monitor_List" />
        <activity android:name="jp.tubes.pbp.jaguar.OrangTua.Activity.LoginOrtuActivity"></activity>
    </application>

</manifest>